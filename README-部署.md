# 撸货记账 · 部署与使用

一个单页应用（PWA），手机 iPhone / 安卓、电脑浏览器都能用，两端记同一本账实时同步。
同步协议与「五行理财」（www0706.netlify.app）完全同源：复用其 Cloudflare Worker 存储，
按独立身份隔离，撸货账本和理财数据互不可见。

## 现役网址（2026-09-19 上线，当前 v32）

**https://1575514641-ship-it.github.io/luhuo-ledger/**

- 托管：GitHub Pages（仓库 `1575514641-ship-it/luhuo-ledger`，public，main 分支根目录）
- 国内直连 github.io 时快时慢：打不开时挂一下代理再刷新；若想换 netlify.app 域名，
  注册 Netlify 后重新部署一份即可（代码无需改动）

## 首次使用（每台设备）

1. 打开上面的网址
2. iPhone：Safari 分享 → **添加到主屏幕**，之后从桌面图标进，就是全屏 App
3. 手机和电脑各生成自己的同步码（设置页可见），把一端的码粘到另一端 → 两端共用同一本账
4. 底部三个标签切换三页：**看板**（总览）/ **账本**（流水）/ **报表**

## 两台设备怎么共用一本账

- 设备 A：右上角 设置 → 我的同步码 → 复制
- 设备 B：设置 → 导入另一台的同步码 → 粘贴 → 导入并配对
- 之后任何一端改动都会自动推到云端，另一端下次打开自动拉取（改动期间也在页面上则会即时同步）
- 换新手机 / 清了浏览器数据：用同步码重新配对一次，账本从云端回来
- 打开页面若弹一次「**云端有更新的账本，本机还有 N 单是云端没有的**」：点「**确定**」＝把这 N 单并进账本（推荐），
  点「**取消**」＝放弃这几单、改用云端版本。本机的单云端都有（正常同步）时不会弹这个框
- 两端同一时间各改了账：云端只认**最后写进去的那一份**（整包覆盖，不是逐条挑），所以别两端同时记同一批单
- iPhone 有个坑：连续 7 天不用，浏览器可能把本机数据清掉。账本在云端，重新配对就回来；
  偶尔用设置页「**导出 JSON**」留一份更稳

## 日常记法

- 状态只有两个：**在途**（垫了钱还没结清）/ **已回款**。不再区分发货、寄出这些过程。
  - **v21 起「自留」已移除**：货自己留着用就不算生意，直接**把那一单删掉**即可（删除＝这单的钱不再出现在任何汇总里，这是这一取舍的代价，想留痕就给它记个备注）。
  - **旧账本里的「自留」单不会被改写**：升级后它们仍是「自留」，显示成「**自留（旧）**」，金额口径不变（利润＝回款−垫付−邮费，仍计入已结算净盈亏，也不算在途）。它们只能出现在「**全部**」筛选里，用户自己决定删还是留——程序不会替你动这笔账。
  - **账本页上面那一行筛选是三个：在途 / 全部 / 已回款**——下面提到的「筛选行右侧」就是这一行的右端（那里是「批量结算」）。
- 右下角「＋记一单」：必填只有**商品名称 + 垫付金额**，日期默认今天、其余全是一键可选，改完即存。
  - 只收了钱、没有垫付的单（比如给别人开发票收到的钱）也走这里：表单**最上面**切到「**收入**」，见下面《记一笔纯收入（开发票）》。
- 邮费怎么记（**v24 起按这个模型**）：
  - **一起寄的一批只有一个邮费**——那个数记在**批次**上（「批量结算」时填的「本批邮费（整批合计）」），
    随时可以改、可以删（见下面「邮费/回款记错了怎么办」）。成员单上显示的邮费只是它按垫付占比
    **分摊到的份额**，专门为了让每单利润展示得出来；**不会**出现「某一个型号单独记自己的邮费」那种情况
    （用户原话：一起寄件它们肯定是一起处理的，邮费肯定就是算在一起的）。
  - **单寄的单才有自己的邮费**：没进任何批次的单，邮费就是它自己的，记在「邮费」栏（垫付金额旁边）或用「编辑」改。
  - 利润＝回款 − 垫付 − 邮费，不填按 0 算。
  - 「整批金额」是这一批邮费的**唯一权威值**：改的时候各单明细一律按它重摊（不是往各单据上叠），
    所以成员明细之和恒等于整批金额，填 0 就是干净地删掉（不留残值）。
- 收到钱：点「回款」，金额默认垫付价，有 **+10/+20/+50/+100** 快捷加价和利润实时预览，确认即入账。
  金额那一栏旁边有个「**清空**」小按钮，点它＝把这笔回款置成 0。
  - **已经回款的单也能改金额（v26 新增）**：卡片上那个按钮在已回款的单上显示为「**改回款**」
    （位置和样式跟「回款」一样，只有文字换成「改回款」，点开的是同一个弹窗），点开时**金额与回款日期都预填现在记着的值**，
    改完保存即生效——**不必删除重记**。它只改这一条记录自己的回款金额/日期，状态保持「已回款」、
    不产生第二条记录，垫付/邮费/数量/批次归属一个字都不动（利润预览照旧＝回款 − 垫付 − 邮费）。
    - 批内单这样单独改回款后，批次表头会如实提示「**≠ 成员明细合计**」——这是**预期**的（同「单独改一张单的邮费」）：
      批次口径要整批改请走「**改本批邮费**」那个入口（弹窗里邮费、回款两个框都能改）。那句告警后面现在还会补一句自助指引
      「要对齐就点『**改本批邮费**』按当前成员重摊一次」（v27），照着点一下就对齐了。
    - **编辑弹窗里看得到本单回款**（v27）：编辑一条「已回款且有回款」的单时，表单里会出现一行只读提示
      「**本单回款 ¥X · 改金额请用卡片上的「改回款」**」——编辑表单里没有回款栏，想改金额别往「垫付金额」里填。
    - 历史遗留的「**自留（旧）**」单**没有**这个入口（它不是回款语义，保持原样只读）。
- 批量结算（v17 新增，v19 起成批留痕）：整批一起寄、对方回一笔总款时，点账本筛选行右侧的「**批量结算**」：
  勾选本次一起寄出的在途单（默认全选），填**本批邮费（整批合计）**和**对方总回款**，
  系统按各单垫付占比自动分摊到每单，并把勾选的单子置为「已回款」。
  回款还没到就把总回款**留空**，先只分摊邮费、单子保持「在途」，等对方回款了再对这批单跑一次批量结算补回款。
- 批次（v19）：结算过的这一批会从此绑在一起——
  - **账本页**：同批的单子收在一条「一起寄出 · 日期」表头下面，表头写整批口径（N 单 / 垫付合计 /
      邮费合计 / 回款合计），单张卡片左侧一道青瓷竖条；卡片里也直接看得到这一单摊到的邮费。
    - **v32 起这个表头可以收起，而且默认就是收起的**：收起时只剩「**一起寄出 · 日期**」+ 右侧「**N 单**」+ 一个箭头
      （▸），下面一行是整批摘要（垫付 / 邮费 / 回款 / N 单在途，为 0 的不显示）——一批批单子看起来就是
      一张张摘要卡，翻账本快得多。**点这一整行**（含箭头）展开成上面说的那一整套：整批摘要 + 「改本批邮费」
      「报单」两个按钮 + 提示行 + 这一批**全部**成员卡片；再点一次收起（箭头变成 ▾）。
      - 展开/收起只切这一块的显示，**不动任何数据**（不进账本、不进同步包）；**刷新页面回到默认收起**。
        同一次打开会话里展开过的批次一直保持展开——哪怕中间改了单、切了筛选（除非刷新页面）。
      - 有「≠ 成员明细合计」告警的批次，收起时摘要行尾部会挂一个红色小标记「**⚠ 待对账**」：
        告警不会被折叠藏起来；完整那句（含「要对齐就点『改本批邮费』按当前成员重摊一次」的自助指引）在展开态。
      - 筛选把这一批的单藏起来时（比如一批 3 单、只看得到 1 单），收起卡上那个「N 单」仍是**整批**单数，
        摘要行尾部会并列一个弱色小标记「**· 本页 1 单**」把本页看得到几单说清楚；展开后还是原来那句完整的
        「本页只显示其中 1 单，另有 2 单被筛选隐藏」。两个标记并排不打架（弱色的在前、红色告警的在后）。
        本页成员全都看得见时不会出现这个标记。
      - 「批量结算」「报单」这些**算账**的入口一律按数据算（当前筛选下的单），与页面上折不折叠无关。
      - 只有 1 单的批次照旧不显示表头（卡片上写「单独一批寄出」），也就没有这个折叠开关。
  - **再次批量结算**：弹窗里这批的单子归在一个「一起寄 · 日期」分组下，点分组那一行的勾选框
     就能**整批勾选/取消**，不用凭记忆一单单挑；分组上还会写「已分摊邮费 ¥45」，一眼看出这批的
      邮费已经摊过、这次只补回款就行（邮费栏留空）。同一批原班人马再结一次 → 续用原批次；
      只勾一半或混着别的单 → 另起一批。
  - **报表页「邮费统计」**：本期邮费合计，下面按批一行行列出「一起寄 · 日期 · N 单 · 邮费 · 回款」
      以及这一批都是哪些货（商品名），单寄没成批的合并成「单寄（未成批）」一行。
      一行里的金额是**成员明细合计**（这一批各单分摊到的邮费/回款加起来）——也就是这一批真实花掉/收回的钱；
      它**正常情况下恒等于**整批金额（v24 纯重摊），只有在「有人事后单独改过某一单」或历史数据的情况下
      两者才会不同，那时行里会补一句「**整批录的是 邮费 ¥X，与成员合计不同（有单被单独改过）**」
      （两个数各自是谁写在明面上，只说明、不改数）。
  - **退出本批（v20，v23 起连钱一起扣）**：结错批了，就在那张单的卡片上点「**退出本批**」，它从此不再和这一批绑在一起。
      整批已经分摊到它头上的邮费/回款**留在它自己身上**照旧算利润（只解绑、不改它的钱）；但它从这一批带走的份额
      会从**还留在批里那几单**的整批口径里扣掉，整批录入值因此恒等于「当前成员实际分摊之和」；
      如果退出后这一批只剩 1 单，那 1 单也一并退出批次（不留「一个人」的批次）。只剩 1 单的批次不再显示「一起寄出」表头。
      （v22 及以前不扣，于是退出后表头会一直挂一句「整批口径仍含它们」式的假漂移，把真正的金额告警顶掉。）
  - **口径怎么看（v21 定稿，v24 起整批与明细恒等）**：**统计只认单子上真实记的邮费/回款**，批次只决定分组与归期：
      - 批次表头一行写的是**整批**口径（标着「整批：」）——v24 起它同时也是这一批的**权威值**
        （各处明细都按它重摊），与成员明细之和恒等；
      - 成员卡片上写的是**这一单分摊到**的明细，报表/看板的邮费都按各单明细汇总：
        「本期邮费合计 ＝ 各批成员邮费之和 ＋ 单寄邮费之和」，恒等于这一期所有单子上记的邮费总和。
        （v20 之前是按整批录入值算的，于是有人「退出本批」时那单的分摊额会被重复计一次——
        v21 全部改成明细口径后这个毛病消失。）
      - 筛选把一批切开时，表头会补一行「本页只显示其中 N 单，另有 M 单被筛选隐藏」；
      - **有人点过「退出本批」或删过批内的单**：表头补一句陈述「**本批已有 N 单不在其中（退出或删除）**」
        （灰蓝，是陈述不是告警）。措辞刻意中性：`batchCount` 只记得到人数，分不出那单是退出还是被删除。
        **退出**那条路 v23 起会把退出者带走的份额从剩余成员的整批口径里扣掉（见上面「退出本批」），
        所以扣完就合得上、不会有后面的「≠」；**删单**那条路不扣（删除＝那一单整笔不再存在，
        整批录入值保持原样，下面的「≠」会如实报出来）。
      - **金额对账任何时候都跑（v22 修，v23 起含义更准）**：表头显示的两项（邮费/回款）只要与成员明细合不上，就标
        「**≠ 成员明细合计 …，与整批口径不同**」（红棕告警色，只提示不改钱）——**与人数有没有变无关**，
        可以和上面那句陈述**同时**出现。退出者的份额会被扣掉（v23 起），所以这句告警基本只在
        「有人事后单独改过某一单的金额」或「删过批内的单」时出现，
        看到它就说明整批值与成员明细**真的**分叉了（v21 会在人数变少时跳过对账；v22 修了这点）。
        改法：在批次表头或那张单卡片上点「**改本批邮费**」，把整批金额重新填一次，明细就都对齐了。
      - 结算时会把**这一批当时结了几单**记在每张单上，上面那句陈述里的人数就是照它报的；
        有人退出后它不再改写（剩下的人还带着旧的人数），原班人马再结一次时才会被改写成新的单数。
- 同款好货：点「再来一单」打开记单表单并**预填原单**（商品/数量/垫付/邮费/渠道/备注），
  日期自动今天、状态在途、回款清空，改一改保存即新建。
- 没出掉：v21 起**直接删掉那一单**（「自留」状态已移除）；老账本里已有的「自留（旧）」单子想删也是点「删除」。
- 0 元购/白嫖单：垫付金额直接填 0，保存时确认一下即可（v9 起支持）。
- 主题：v14 起定稿「青瓷薄荷」浅色玻璃风（绿松石 #14b8a6/#2dd4bf，固定浅色，手机开深色模式也用浅色）。
- 报表页：**月 / 季 / 年** 切换 + ‹ › 翻期，自动生成总结（单数、垫出、收回、净盈亏、环比上期），
  季报/年报带分月盈亏柱状图；页面上的图共三张（「**现金流走势**」「**净盈亏分布**」「**商品利润占比**」），另有一张「**邮费统计**」卡与已结算利润 Top5 的「**商品榜**」。
- 看板页重点盯「垫付在外未回笼」大字卡（垫出去还没回来的钱）；「本月垫出」格下另有一行本月邮费，
  大字卡的提示行也会补一句「**另有邮费 ¥X**」（在途单自己记的邮费合计；那个大字本身只算垫付，不包含邮费）。
  - **这两行「另有邮费」不是一个数**：大字卡那行是**还在途的那些单**记的邮费合计，「本月垫出」下面那行是**本月**记掉的邮费——两者本来就不该相等。
- **口径（v27 统一后的写法）**：**一笔钱算不算收入，只看「回款金额填了没有」（非空即计入），与状态无关**；
  归期看**回款日期**（没有回款日期时只进「累计回款」，不落进任何一个具体月份）。
  所以看板的「累计回款/本月回款」与报表的「回款」永远是同一个答案——v27 之前看板按「回款非空」、
  报表按「已结算且有回款日期」，一条「已回款」的单被编辑成「在途」（回款字段留着）后两处会给出不同的数。
  利润跟着收入一起认（认了这笔收入就认这笔盈亏），垫付/邮费照旧按各自的下单日期/批次日期归期。

## 报单（v28 新增，v31 起带快递单号）

寄出之后要给收货商报「型号 / 颜色 / 数量」时用这个——**点一下就整理好并复制到剪贴板，直接去微信粘贴**。

- **两个入口**：① 账本页「一起寄出 · 日期」那条**批次表头**上的「**报单**」→ 报这一批的全部成员；
  ② 筛选行右边的「**报单**」→ 报**当前筛选下看得见的那些单**（所见即所报，范围会写在面板标题后面）。
- **点一下＝已经复制好了**：面板打开的同时剪贴板里就有内容（提示报「已复制报单 · N 件 / M 种」）。
  面板是用来核对与微调的，不看它也能直接去微信粘。
- **文本长这样**（照你发给收货商的那种消息）：

  ```
  9.21 待结
  快递单号 SF1234567890
  狗屁王二代金刚黑 ×5
  狗屁王 4 朱雀黑 ×2
  狗屁王 3 黑色 ×1
  ```

  首行「月.日 待结」——成批报用**这一批自己的日期**，工具条入口用今天；下面**一行一件**，
  **每行行尾都带数量**，没有合计行。想加称呼、想改数量的写法，直接在面板的文本框里改，
  再点一次「复制报单」：**复制出去的就是框里现在的文字**（这种手改过的复制，提示只说「已复制」、
  不再报件数——件数是从勾选重算的，跟框里的内容已经对不上）。另外**一动勾选、或一改单号那一格，
  框里内容会重算、手改的部分会被覆盖**，所以改完请先复制走。
- **快递单号（v31 新增）**：面板里那一格就是报单文本第 2 行的值；
  - 打开面板时按**这个范围里最常见的非空单号**自动预填（同一次寄件通常只有一个单号，填过一次以后就不用再打）；
    预填只是「省你打字」，它本身**不动账本**；
  - **那一格下面的小字**（按**本次报出去的这几单**现算，改完单号/改完勾选它立刻跟着变，不会留着已经过时的话）：
    · 本次报的单里有 **2 种以上**单号（例如同批里有一两单补录了另一个包裹的号）→
      「本次报的单里有 N 个不同单号：a / b / c（等共 N 个）——文本里只带上面那一格的值；要统一就改这一格」；
      同时开面板那句提示会变成「单号有 N 种，先核对面板里的提示再报」——因为这时**自动复制出去的文本
      用的是那个众数**，少数派那几单的号并不在里面，直接粘给收货商他会查不到件；
    · 本次报的单里只有**一种**单号、而这一格填的是**别的**号（典型：你把记着别的号的那几单取消了勾选）→
      「上面这一格填的单号不在这次报的单里（它们记的是 X）；要改它们就改这一格」——这时文本里带的那个号
      不属于任何被报出去的单，收货商按它同样查不到件；
    · 本次报的单**一个号都没有**（新寄的货还没抄号，最常用的路径）或两边本来就一致 → **不显示任何小字**；
  - **改它才算数**：改完这一格（鼠标/手指离开输入框、或按回车）会做四件事——
    写账本（只写单号）→ 重算文本 → **重新复制一遍剪贴板** → 提示「单号已记到 N 单 · 已重新复制」。
    这一步是必须的：不做的话剪贴板里还是打开面板那一刻的旧文本，而提示还在教你「直接去微信粘贴」；
  - **清空它**（右边的小「清空」按钮）＝把这次报的这几单的单号去掉，文本里那一行整行消失（与「改它」同一条路）；
  - **只动那一格是不够的**：它还必须是「**你自己改过它**」。光打开面板、光点「复制报单」，一个字都不会写进账本——
    预填是众数，少数派那几单与它天然不同，若按「值不同就写」实现，你「什么都没改、只想再复制一遍」
    就会把那些单的正确单号统一成众数（静默串单，且账本没有撤销）。
- **单号会记进账本**：这是面板里**唯一**会写账本的地方（也是 v28 那句「面板是只读的」唯一的例外）。
  写进去以后，再打开面板它会自动带着，账本卡片那一行也看得到「· 单号 X」——改错了就去那一单的「编辑」里改回来。
  单号也可以在**记单/编辑表单**的「更多（状态）」里填（那一格是货单形态专属，收入模式下不显示；
  编辑时不动它、不展开那一栏直接保存，原单号也不会丢）；
  **「再来一单」不会把原单的单号带过来**——那是新的一次寄件，单号一定不同。
- **同名的并成一行、数量相加**：同一件货记过几单（例如从不同店买的同款）会合成一行 ×N。
  「件数」按每条单自己填的**数量**相加，**不是订单条数**。
- **写法不一致也会并**：`红米 Note 13 白` 与 `红米Note13白`（空格、大小写、全角空格不同）算同一组，
  面板里会写一句「已把同物异写并成一组：…」让你看见它并了什么；显示用你**最早那条单的原始写法**。
  `白` 与 `白色` **不会**自动合并——宁可多一行让你手动删，也不去猜颜色（猜错等于把两种货报成一种）。
- **要剔掉某一单、或一批拆两次报**：在面板的清单里取消勾选那一单（默认全勾），文本立刻重算。
- **面板除单号那一格外都是只读的**：勾选、改文案都不动账本、不进同步包，关掉即丢；账本内容要改请去编辑那一单。
  单号那一格如上面几条所述——**只有你自己改了它**才写账本（改了它＝同时改了那几单的号，也就同时会进同步包）。
  另外收入单（开发票）如果落在报单范围里也会被当成一行报出去——它没被特殊处理，用不上就取消勾选。
- **加过单号之后，手机和电脑要一起更新到 v31**：老版本不认得 `tracking` 这个字段，它打开账本时会把这个字段
  剥掉、再整本推回云端——于是单号集体消失。按「攒一批再发版」的节奏，中间那段版本错位窗口里
  **旧版本那台机器先别打开这个账本**（打开即可能把单号抹掉），两台都更新到 v31 之后再正常用。

## 邮费/回款记错了怎么办（v23 新增，v24 补全入口）

按下面几种情形对号入座（整批部分的回款同理，把「邮费」换成「回款」即可）：

| 你的情况 | 入口 | 怎么做 |
| --- | --- | --- |
| **单寄的一单**（没成批）记错了 | 那张单卡片上的「**编辑**」 | 改「邮费」栏 → 保存；**留空就是删掉它**（按 0 算），旁边有个「**清空**」小按钮一键置空。邮费栏本来就不是必填 |
| **就这一单收到的钱记错了**（含纯收入单） | 那张单卡片上的「**改回款**」 | 点开时金额与回款日期都预填现在记着的值 → 改成正确的数字 → 确认。只改这一单，不会多出一条记录（旧「自留（旧）」单没有这个入口） |
| **整批一起寄的那一批**记错了 | ① 账本页那条「一起寄出 · 日期」表头上的「**改本批邮费**」；② **同一批里每张单的卡片上**也有同一个按钮 | 点开弹窗已经替你勾好这一批全部成员 → 填新数字 → 结算。寄出去后才发现邮费变了，就是这条 |
| **想彻底删掉这一批的邮费** | 同上 | 同上，但新数字填 **0** = 删掉这一批的邮费：成员明细也一并归 0、表头不再显示邮费、报表与看板同步归零 |
| **不确定那笔钱花在哪** | 报表页「**邮费统计**」卡 | 本期邮费合计下面按批一行行列着这一批**都是哪些货**（商品名）与金额，单寄的合并成「单寄（未成批）」一行 |

**入口在哪（一句话）**：除了「一起寄出 · 日期」那条**批次表头**右侧，**每张批内单的卡片上**（「退出本批」旁边）
也有「**改本批邮费**」——两者点开的是同一个弹窗。v23 只有表头有，于是①一批只剩 1 单时不显示表头、
②筛选把那一批藏起来时表头不在页面上——这两种情形下都点不到，v24 补上了。
（另外：批内单在「编辑」里也能单独改它的邮费，那只是改这一单的展示值，会和整批金额不一致，
表头会用「≠ 成员明细合计」如实提示——要改这一批就请走上面的入口。）

三条规矩（v24 起弹窗里就这么三条，**没有「覆盖 / 追加」要选**）：

- **填数字＝把这一批改成这个数**：整批金额是这一批邮费的**唯一权威值**，成员明细按各单垫付占比
  重新分摊（**不是**往原来记的数上叠加，也不会保留什么「这一单自己记过的邮费」——一起寄的一批只有一笔邮费）。
  同一个数字填两遍，结果一样（幂等）。
- **留空＝一个字不动**。弹窗里两个框（本批邮费 / 对方总回款）谁留空谁不动，所以
  「先只摊邮费、回款到了再补一趟」照旧：第二趟只填回款、**邮费留空**，已经记好的邮费不会被清掉。
- **填 0＝删掉这一笔**：邮费填 0 → 这一批的邮费清零（成员明细一起归 0、报表/看板同步归零）；
  回款填 0 同理（单子仍是「已回款」，收没收到钱是另一回事，要改状态去「编辑」里改）。
- 新建批次（还没有旧批）不显示那行说明，直接按新批次写入。

## 记一笔纯收入（开发票）（v25 新增）

「只收了钱、没有垫付、没有成本」的生意（给别人**开发票**收到的钱就是典型）不必再假装成一单货——
这一节只管**把到账的钱记进总账**，发票怎么开、税怎么算，它一概不做：

- **怎么进**：右下角「＋记一单」→ 表单**最上面**多了一条「**货单 / 收入**」的类型切换 → 点「**收入**」。
- **填什么**：只剩 **名称 / 金额 / 日期 / 渠道 / 备注** 五项（默认名与默认渠道都预填「**开发票**」，
  日期默认今天，渠道不是必填）。垫付/邮费/数量/状态那四项会**整格收起来**（不是置灰）——
  它们不参与校验也不参与提交，所以不用管。
  - 名称留空、金额留空、金额为负 → 拒绝保存并提示；**金额填 0 是合法的**（一笔 0 元收入），
    与「留空」不是一回事。
  - 收入单保存**不会**弹那个「垫付金额为 0」的确认框——它只服务**货单路径**，货单垫付 0 时照旧会弹。
    （v26 起那句话改成中性措辞：「**这一单垫付金额为 0（白嫖单或纯收入单），确认保存吗？**」——
    因为**编辑**一条已有的收入单会按货单路径回显、垫付恒为 0，那次确认是必然要出现的，
    老文案「确认这是 0 元购/白嫖的单子吗」在那条路上读不通。刻意保留这次点击、只改措辞。）
  - 切回「货单」时字段原样恢复，刚才填过的值都还在（隐藏只是收起来，不会清掉）。
- **落到哪些字段**（一句话：**不落任何新字段**）：金额写进 **回款金额**（`income`），日期同时写进
  **下单日期**（`date`）与**回款日期**（`incomeDate`）——你只填一个日期，它既是发生日也是到账日；
  状态直接是「**已回款**」、垫付与邮费都是 **0**、数量 1。所以这条记录在账本、同步、备份里与一单普通
  货单**完全同形**，老账本一行都不用迁移，也不存在「类型」这种字段。
- **在报表里怎么看**：**没有单独的「收入」分块、列或图例**——这笔钱直接并进现有的
  **总收入（回款）与净利润**（净利润 = 逐单「回款 − 垫付 − 邮费」求和，收入单的贡献恰好等于它的金额），
  **成本侧（垫付/邮费）一分都不会多**，按你填的那个日期归期（跨月就落在所选的月份）。
- **想看都有哪些收入**：账本页「**全部**」/「**已回款**」里能看到这些卡片（垫付 ¥0、邮费 ¥0、
  利润 = 那一笔金额）；报表「**商品榜**」里会出现一条以你填的名称（默认「开发票」）为名的条目。
  「再来一单」可以拿它当模板再记一笔。
- **改错了怎么办**：点卡片上的「**编辑**」改名称/日期/渠道/备注（打开时按普通货单形态回显：
  垫付 0 / 邮费 0 / 数量 1 / 已回款，直接保存不会丢金额）。**金额走卡片上的「改回款」**（v26）：
  点开时金额与回款日期都预填现在记着的值，改完保存**只改这一条的回款金额与回款日期**——
  不新增记录、不动垫付/邮费/数量/批次，**不必删除重记**。
  （顺带修掉一个老毛病：垫付为 0 的单以前点开「编辑」时垫付栏是**空白**，而它带必填 →
  什么都不改点保存会像卡住一样没反应；v25 起 0 就写成 0，直接保存能过。）

## 技术备忘

- 文件：index.html / app.js / sync.js / styles.css / manifest.webmanifest / icon.svg / icon.png
- 同步接口默认 `https://www0706.netlify.app/api/sync`（理财站的 Netlify 反代，已放开 CORS `*`，
  任何静态托管都能直连）；如需换接口，设置页可覆盖（localStorage `luhuo-sync-endpoint-v1`）
- 身份与同步码：`LUHUO1.` 前缀 base64url(userId+secret)，与理财站的 `WUXING2.` 互不相通
- 本地存储键：`luhuo-ledger-data-v1` / `luhuo-ledger-meta-v1` / `luhuo-sync-identity-v1`
- **金额负值收敛（v22）**：`cost / fee / batchFee / batchIncome` 在 `normalizeData()` 里**负值一律收敛为 0**
  （只在 <0 时动手，0 与正数原样保留）。表单本来就拦负数（`min="0"`），能带进负值的只有**导入 JSON**
  与**云端拉取**两条路；而负的 fee 会让两处自相矛盾——报表「本期邮费合计」只累加 >0 的 fee（显示 ¥0），
  卡片上却照原样显示「邮费 −¥3」。`income` **不在**收敛范围内（负回款在各处口径一致，没有这种矛盾）。
  v23 起 `batchFeeShare` / `batchIncomeShare` 也走同一规则（负值归 0），另加取整——它们的单位是**分**。
- **每单的批次份额（v23）**：`batchFeeShare` / `batchIncomeShare`（**分**，整数）记这一单在整批邮费/回款里
  **摊到多少**，与 `batchId/batchDate/batchFee/batchIncome/batchCount` 一样冗余在每张单上。
  v24 起它撑起两件事：①改本批金额时与成员 fee 一起被重写成新份额（成员 fee = 份额/100，恒等）；
  ②退出本批、只勾部分批内单另起一批时按份额扣减原批次的整批口径。
  **老数据（v23 之前）没有这两个字段**（读出来是 0，但整批数字还在）：只在**退出/重组扣减**那两条路上
  按当时的分摊规则（权重＝各单垫付、在分层面取整）用整批数字**反推一次**。反推只在**该批份额全为 0**
  时发生；份额有值就相信单上的记录（那种情况合计对不上＝后来被人改过或删过单，不能拿整批数字重算，
  否则会把改过的钱抹平）。**v24 的「改本批金额」不用反推**：新份额直接按权重算出来赋值，老批次一样一次到位。
- 冲突策略：整包 last-write-wins（与理财站一致），适合单人多端"哪边最后改听哪边"
- 升级与发布代码：改完把 index.html 里的 `?v=N` 加一，强刷缓存；发布＝改 `webapp/` 里的文件 → 同步到 `E:\杂活\撸货记账\site-repo\` → `git push`，Pages 自动重构建
- v4（2026-09-19）：状态简化为在途/已回款/自留（旧六态自动迁移），去掉垫付方式（钱包）维度，
  新增报表页（月/季/年 + 自动总结 + 分月柱状图 + 商品榜）、再来一单、回款快捷加价与利润预览
- v17（2026-09-19）：新增批量结算（多单整批寄出/整笔回款，按垫付占比在「分」层面整数分摊邮费与回款）；
  「再来一单」从直接复制改为打开表单预填原单信息
- v19（2026-09-20）：批量结算**留痕成批次**（v18 为同日中间版本：同一批功能，仅弹窗排序微调）——订单上记 batchId + 整批邮费/回款（冗余在每张单上，
  删单不留孤儿）；账本页同批单子按「一起寄出」分组显示、批次表头走整批口径、卡片直接显示本单摊到的邮费
  （金额栏改成两列，垫付/回款 + 邮费/利润）；批量结算弹窗按批次分组、分组勾选框一整批一键勾选、
  显示「已分摊邮费 ¥X」，整批原班人马再结一次续用原批次（只勾一半则另起一批）；
  报表页新增「邮费统计」卡（本期邮费合计 + 按批明细含商品名 + 单寄汇总），报表总结句也带上本期邮费
- v20（2026-09-20）：批次功能的**边界加固**，不新增流程——
  ①「退出本批」：批内单卡片上新按钮，结错批可把某单摘出（只解绑，已分摊到它的邮费/回款留在它身上；
  退出后该批只剩 1 单时，那 1 单也一并退出）；
  ②表头口径写明「整批：」，筛选把一批切开时补一行「本页只显示其中 N 单，另有 M 单被筛选隐藏」；
  ③手工改过批内单邮费、或单给某单回款后，表头标「≠ 成员明细合计 …，与整批口径不同」（只提示不改钱）；
  ④删批内单的确认框带上批次信息（属于一起寄出的 N 单之一 / 删完还剩几单）；
  ⑤只剩 1 单的批次不再显示「一起寄出」表头；同批单子聚成一块，散单不再被夹在批次表头和成员之间；
  ⑥顺手修两个老 bug：订单状态「自留」刷新/云同步后被错误打回「在途」（v4 起就存在）、
  筛选选「全部」刷新后被重置成「在途」；
  ⑦看板「本月垫出」格下补一行「另有邮费 ¥X」（归期同报表页：整批按批次日期、单寄按下单日期）
- v21（2026-09-20）：**统计口径修正 + 状态模型收敛 + 手机排版**
  ①**邮费统计口径改为「成员明细合计」**（真实缺陷修复）：报表页「本期邮费合计」与看板「另有邮费」
  一律按**各单自己记的 fee** 汇总，批次只决定分组与归期、不再作为统计取值。
  改前（v20）：成批的按 `max(batchFee)` 算 + 单寄按 fee 算——于是「退出本批」后，退出那单的分摊额
  既留在剩余成员的整批口径里、又作为单寄单被计一次，**邮费被重复计**（3 单摊 ¥21 + 散单 ¥6 应为 ¥27，
  退出 1 单后显示 ¥34）；反过来，成员入批前自己记过邮费时（submitBatch 对邮费是有意累加）又会**少算**。
  改后恒有：`本期邮费合计 = Σ各批成员 fee + Σ单寄 fee = 本期归期所有单子的 fee 之和`。
  批次表头仍显示**你录入**的整批数字（有对账价值）；报表批次行在同一口径下补「整批录入 邮费 ¥X」备注。
  ②**「退出本批」不再误报口径漂移**：结算时把当时的成员数冗余记成 `batchCount`（老数据缺失＝0），
  表头据它分两种措辞——有人退出 →「已有 N 单退出本批，整批口径仍含它们」（陈述）；
  成员数没变却合不上 → 仍是「≠ 成员明细合计 …，与整批口径不同」（告警，换成红棕色，v20 用的灰蓝
  和普通提示几乎同色肉眼分不出）。
  ③**移除「自留」状态**（用户决定）：状态只有在途/已回款，记单/编辑的下拉、账本筛选（三个 chip：
  在途/全部/已回款）都不再提供自留。**旧数据绝不静默改写**：已有的「自留」（含更老的「翻车自留」）
  原样保留，显示成「自留（旧）」，金额口径不变（仍计入已结算净盈亏、不算在途），只能在「全部」里
  看到并自行删除；看板环形图与状态行按「现役两态 + 数据里真有的遗留态」渲染，各块垫付之和恒等于
  「垫付合计」（占比不会因自留不在状态表里就少一块）。代价：用户删掉自留单后，那笔钱不再出现在
  任何汇总里——这是用户接受的取舍。
  ④手机筛选行排版：chip 内边距 6px 14px→6px 10px、gap 8px→6px，并加 `flex-wrap` 兜底
  （去掉原来的横向滚动裁切），390/360/430px 三档下三个 chip 与「批量结算」都完整落在一行、无横向溢出。
  ⑤单成员批次的卡片上补一句「单独一批寄出」（它不显示批次表头，光有「退出本批」按钮看不出上下文）。
- v22（2026-09-20）：**定点修正（三处 v21 遗留的小问题 + 两处探测出来的真问题）+ 测试加固**
  ①**批次表头的金额对账不再被跳过**（真实缺陷，只影响提示、不影响金额）：v21 在「当前成员数 < batchCount」
  时直接返回陈述句、**跳过金额比对**——于是 3 单同批（整批邮费 ¥12、各单 ¥4）删掉一单、再把另一单邮费
  改成 ¥99（整批 12 vs 成员明细 103，差 91 元）之后，表头**仍然只有那句陈述、不再告警**。
  现在金额对账**永远要跑**：人数少的陈述句与「≠ 成员明细合计 …」告警可以并存（两条 .bh-note）。
  ②**「退出本批」措辞中性化**：`batchCount` 分不出「退出本批」与「删除该单」，陈述句不再断言「退出」，
  改为「**本批已有 N 单不在其中（退出或删除），整批口径仍含它们**」，仍是陈述语气（灰蓝，非告警）。
  ③**金额负值收敛**（见上面「技术备忘」那条）：`cost / fee / batchFee / batchIncome` 负值归 0。
  改前一条负 fee 的脏数据会让卡片显示「邮费 −¥3」而报表「本期邮费合计」是 ¥0，两处自相矛盾。
  ④横向裁切探测（本轮新判据）顺带查出并修掉的两处真问题：
  - **商品榜的比例条一直是坏的**（v4 起就存在）：`.prod-row` 与 `.status-row/.month-row` 共用了一条
    `display:flex`，比例条的轨道被挤成 1–3px——五行的「条」都是一个点，等于没画。改成 `display:block`
    后比例条回到行的下方、占满整行（100%/60%/40% 如实显示）。
  - **手写 SVG 图的默认 `overflow:hidden`**：年视图首尾那两颗数据点、以及「4月/9月」这类贴边刻度
    各被切掉约 2px。给 `#chartFlow`/`#chartProfit`/`#dashFlow` 的 svg 放开裁切（溢出仅 ≤3px，
    落在卡片的内边距里，观感不变）。
  ⑤看板「已结算净盈亏」的提示文案由「已回款＋自留」改为「**已回款＋旧自留**」（v21 起「自留」只可能是旧数据）。
  ⑥**冒烟测试 40 → 51 条**：新增「删单后对账仍跑」守门断言、中性措辞断言、负值收敛断言、批量结算弹窗的
  版面判定，并把横向裁切判据换成「元素 vs 它**最近的会裁切祖先**的内边距盒」——旧判据两条都有结构性盲区
  （`documentElement.scrollWidth - clientWidth` 被 `body{overflow-x:clip}` 夹成恒 0；逐元素「右边界超视口」
  要先跳过祖先里有 overflow 容器的元素，而 body 自己就是，于是每个元素都在 body 那层被跳过）。
  另加两个**变异反证**（M1：360px 宽元素塞进 120px 的 `overflow:hidden` 容器，自身右边界仍在视口内；
  M2：筛选行还原成 v20 的 `overflow-x:auto`+`nowrap`）证明新判据不空转——变异时报出溢出、恢复后全绿。
- v23（2026-09-20）：**补上「邮费/回款记错了怎么改、怎么删」的修正路径**（用户实际用出来的缺口：
  「显示我目前有 31 元的邮费…想更改之前记录的邮费，我找不到入口」）＋顺带修掉复核记的 D-3
  **【已失效 → 见 v24】**：本版引入的「覆盖 / 追加」两个选项与「自带部分」概念在 v24 被整体删除
  （用户拍板「我肯定是直接把它完全更改，这并不需要我去重新选」），下面 ②⑤ 两条只作历史留档；
  `batchFeeShare` / `batchIncomeShare` 这两个字段本身保留，作用改为「退出/重组时的扣减依据」。
  ①**每单多记 `batchFeeShare` / `batchIncomeShare`**（分）：结算时真正摊到它头上的份额，与整批口径同一套
  冗余方式。由此「**这一单自带的邮费** = fee − batchFeeShare/100」可精确算出，覆盖/清零/退出扣减全靠它。
  `normalizeData` 认这两个字段（缺失兜底 0、负值归 0、取整），`submitForm` 编辑时原样保留；
  v23 之前的老批次没有这份记录，覆盖/退出时按「整批数字 × 各单垫付权重」反推一次（见「技术备忘」）。
  ②**整批邮费/回款可覆盖、可清零**：所勾选单恰好是某批的整批原班人马且该批已有金额时，弹窗出现
  「**改成新值（覆盖）**（默认）／ **追加到原有**」两个选项。**留空＝不动**（保住「先只摊邮费、
  回款到了再补一趟」的两趟打法）；覆盖＝按新总额重新分摊、成员 fee = 自带部分 + 新份额、`batchFee` = 新总额、
  份额同步改写，**幂等**（同输入跑两遍结果一样）；**填 0 覆盖 = 删掉这一批的邮费**（成员回到自带邮费、
  `batchFee=0`、表头不再显示邮费，回款同理）。新建批次不显示这个选择，按新批次写入。
  ③**批次表头加「改本批邮费」入口**：点一下直接开批量结算弹窗并**预选该批全部成员**（含已回款的——
  弹窗平时只列在途单，收过款的那批以前根本点不到），于是「寄出去后发现邮费变了」＝
  表头点一下 → 填新值 → 覆盖。按钮走青瓷系、30px 高（手机点得到），挂在整批口径那一行的右侧。
  ④**修掉 D-3（整批口径含已退出者）**：`leaveBatch()` 把退出者带走的份额从**剩余成员**的
  `batchFee/batchIncome` 里扣掉；只勾批内一部分人另起一批时，原批次同样扣掉被带走成员的份额。
  于是整批录入口径恒等于「当前成员实际分摊之和」——退出后**不再出现那句假漂移**，也不再因此把真正的
  金额告警顶掉。表头那句陈述随之去掉「整批口径仍含它们」半句（退出扣了、删单没扣，那句话已不准）；
  `batchCount` 语义不变（结算那一刻的人数）。注意：**删单**不走扣减（那一单整笔不存在了，整批录入值保持
  原样，由「≠ 成员明细合计」如实报出来）——这是本版划的边界，不是遗漏。
  ⑤覆盖时兜了一下底：自带部分为负（有人把某单邮费改到比它摊到的还低、或老批次反推出来的份额）时
  按 0 收，避免把负数写进账本（负数只可能来自脏数据，而卡片显示 −¥3、报表 ¥0 这种自相矛盾正是 v22 修的）。
  ⑥**冒烟测试 51 → 63 条**：新增 12 条 v23 断言，全部走真实 UI（点表头入口 → 填值/选口径 → 结算 →
  查数据与报表）：入口与点击区域、弹窗预选与默认覆盖、覆盖重算、幂等、留空不动、追加累加、清零与
  报表/看板归零、退出按份额扣减不报 ≠、单寄单编辑留空归 0、**老批次（无份额字段）覆盖时反推**。
  另按新语义改写了 13.2/13.3（退出后不再是「陈述＋告警并存」，而是扣减后合得上、只剩陈述）。
- v24（2026-09-20）：**把「改/删这一批的邮费」真正做通**——入口补全 + 删掉「覆盖/追加」的选择 +
  改成纯重摊 + 钉住「一批一笔邮费」的领域模型（用户实际用出来的缺口：v23 加了能力却找不到入口，
  以及「填 0 覆盖后 31 元没被覆盖掉」）
  ①**入口补全（真实缺陷）**：v23 的「改本批邮费」只在**批次表头**上，而 ⑴单成员批次不渲染表头
  （`renderList` 里 `grouped = g.count > 1`）、⑵筛选把那一批藏起来时表头不在页面上、
  ⑶主「批量结算」入口只把 `status === "在途"` 的单放进 pool，**已回款的批次一进去就是空的**——
  三条路一起把用户堵死在「找不到入口」上。现在**每张批内单的卡片上**也有同一个「改本批邮费」
  （单成员批次、已回款/旧自留成员照样有；按 `o.batchId` 渲染，与筛选、与这一批还剩几个人都无关），
  走 `#orderList` 既有的 `data-act` 委托，动作与表头那个完全一样；表头入口保留。
  ②**去掉「覆盖 / 追加」的选择，改成纯重摊**（用户拍板「我肯定是直接把它完全更改，这并不需要我去重新选」）：
  `#batchMode` 那两个 chip、`batchMode` 状态、`append` 分支、`ownCentsOf()` / `batchShareMap()`
  全部删除；弹窗里只剩一行说明「填数字＝把这一批改成这个数（按各单垫付占比重摊到成员上）／
  两个框留空＝不动／填 0＝删掉这一笔」。**整批金额是这一批邮费/回款的唯一权威值**，成员明细一律按它重摊
  （`成员 fee = 新份额`，不再叠加任何「自带部分」），所以①幂等（同数字填两遍结果一样）②清零清得干净
  ③老批次（没有份额字段）也不用反推——一次到位。
  ③这条同时**修掉用户那条「31 元清不掉」**（真实缺陷）：他那份数据是 8 单、成员邮费各约 ¥4（合计 ¥32），
  而整批录入值只有 ¥1；v23 的 `成员 fee = 自带部分 + 新份额`（自带部分 = 现 fee − 按 ¥1 反推出来的份额）
  会把每单那 ¥4 当「单子自己入批前记的邮费」留下来，**填 0 之后总额仍是 ≈¥31.8**。
  v24 纯重摊后填 0 就是 Σ = 0。④**报表行措辞**：那句补充说明由「整批录入 邮费 ¥1」（容易被读成
  「这一批的邮费是 ¥1」，而同一行上面明明写着成员合计 ¥32）改成
  「**整批录的是 邮费 ¥X，与成员合计不同（有单被单独改过）**」，且只在两者**确实不同**时才出现。
  ⑤**表单里的「清空」**：「找不到删除入口」的另一半原因是「删除＝把输入框留空」这件事没人猜得到——
  记单/编辑的**邮费**栏、回款弹窗的**回款金额**栏各加一个小「清空」按钮（点邮费＝置空，留空按 0 算的老口径；
  点回款＝置 0，因为那个框带 `required`，置空会被浏览器拦下、点确认会像卡住一样）；按钮用绝对定位挂在
  标签那一行的右端，**不进文档流**，所以 `.row2` 两格输入框仍然严丝合缝对齐（`label.control` 仍是 input，
  按钮排在 input 之后——button 也是可关联元素，排前面会把「邮费」这个标签抢到按钮上）。
  ⑥**编辑批内单时补一句归属说明**（用户的困惑点就是他一直在找「每一单的邮费」）：邮费框下面写
  「这一单属于一起寄出的那一批（日期），邮费在批次那行、或这张单卡片上的「改本批邮费」里统一改；
  在这里单独改只会让它和整批口径不一致」（未成批的单不显示）。邮费框**仍然可改**，只是加提示——
  单独改出来的差异由表头的「≠ 成员明细合计」如实提示。
  ⑦**冒烟测试 63 → 77 条**：新增/改写覆盖「单成员批次（无表头）的卡片入口与预选」「已回款批次的卡片入口
  （并对照主入口进不去）」「清零清得干净（Σ = 0）」「填数字一律替换且幂等」「编辑表单的清空按钮
  （置空/置 0、点区几何、label 关联、两格对齐）」「**用户那份 8 单真实数据形状的回归**
  （报表行措辞 + 填 0 归零 + 重填 45 按权重重摊）」以及三档宽度（360/390/430）下账本页 + 四个弹窗
  的碰撞/裁切复验；按 v23→v24 的新语义改写了 13.3/13.4/13.6/18.1/18.2/18.3/18.5/18.9/18.10。
- v25（2026-09-20）：**新增「记一笔纯收入（开发票）」**——只有收入、没有成本的生意（给别人开发票收到的钱）
  不必再假装成一单货。
  ①**只在记单表单顶部加一个类型切换**（沿用报表页 `.segment` 那条样式：货单默认 / 收入）。选「收入」时
  表单只剩 **名称/金额/日期/渠道/备注**：垫付/邮费/数量/状态**整格 `display:none`**（不是置灰），
  隐藏字段**不清空其值、不参与校验也不参与提交**；`#orderForm.kind-income .row2` 把剩下的可见格
  （金额、日期）由两列**收成一列**，免得留半张空栅格。名称与渠道各补一个默认值（都预填「开发票」），
  但只在**新建表单、且用户没碰过那一格**时补（`nameAuto` / `channelTouched` 两个标记）——
  切回货单会把我们替他填的那个默认名收回，用户自己填的值一律保留（切类型不清值）。
  ②**保存按收入语义写现有字段，绝不新增类型字段**：`cost=0`、`fee=0`、`qty=1`、`status="已回款"`、
  `income=金额`、`incomeDate=date=用户只填的那一个日期`。这正是本版的**红线**：老账本零迁移，
  任何靠「新字段缺省」或「`cost===0` 之类宽松判断」去识别收入单的写法，都会把「垫付 0 + 已回款」的
  **老货单**误判成收入单并静默改写它的语义（下单日期/回款日期被合并、数量被归 1）。所以类型**只是表单模式**
  （`formKind`，随表单关闭即复位），落库后与货单同形——于是这笔钱天然并进现有的总收入/净利润，
  报表**不需要任何新分块/列/图例**（用户口径：合并进总账）。
  ③**「0 元购」确认只服务货单，正反两向都保住**：收入单天然 `cost=0`，保存路径在读到金额后
  直接入库、**不经过**那句确认；货单垫付 0 时照旧弹（编辑路径也一样）。
  ④**校验**：名称留空（含纯空格，trim 后为空）与金额留空都拒绝并提示（原生 `required` +
  保存前校验两层）；**填 0 是合法的 0 元收入**（与留空分开处理）；金额为负拒绝（`min="0"` + 保存前校验）；
  精度/上限沿用全站口径（存值 `numberValue()`、显示 `money()` 两位小数、不新增第二套规则）。
  ⑤**编辑一律按货单形态回显**：编辑既有记录时**整条类型切换隐藏**，垫付/邮费/数量/状态照常显示
  （值就是 0/0/1/已回款），`submitForm` 对既有记录原样保留 `income`/`incomeDate`——编辑不会把收入单的
  金额弄丢，也不会把它拆成「下单日 + 回款日」两个日期。顺带修掉一个老毛病：`f.cost.value / f.fee.value`
  以前写的是 `src.cost || ""`，**0 会被变成空串**，而垫付那格带 `required` → 「编辑一条 0 元单、
  什么都不改点保存」会被浏览器原生校验拦死（点保存像没反应）；现在 0 就写成 0。
  ⑥**连点保存**：表单已关之后的提交一律丢弃（`submitForm` 开头看弹窗还在不在），保证「一次保存 → 恰好一条」。
  ⑦**冒烟测试 77 → 117 条**（一条老断言都没删；77 条老断言逐条仍在跑）：新增 40 条，按千问那 27 条用例的编号对齐——
  A1/A2/A3 表单收缩·来回切换不清值·默认值可改；A4–A7 名称/金额留空、非数字、负数（每例都跑两遍：
  原生校验 + `noValidate` 绕过原生后由保存前校验拦住）；A8 0 元收入（confirm 计数 0、报表 +0）；
  A9/A10 两位小数与百万/近亿（不出现科学计数法与尾数）；A11 中间空格与 emoji 往返；B12 **直接读
  localStorage 的 JSON** 断言字段映射并比对**字段集合逐字等于货单**（零新字段的铁证）；B13/B14 确认框
  正反两向；B15 老货单改造成「像收入单」仍是货单；C16 合并进总账（+1000 与报表分块指纹逐项不变）；
  C17 跨月归期；C18/C18b 收入单对成本侧贡献恒为 0；C19 同一天连记 3 笔；C20 快速两笔 = 两条、双击 = 一条；
  C21/C22 在账本状态筛选（在途/全部/已回款）与看板上的落位（本 app 没有渠道/日期范围筛选，那两条不适用）；
  C23/C23b 编辑回显与不丢值、金额改动走「删除重记」的路径；D24/D24b/D25 零迁移（老账本数字复算一致、
  老 0 元已回款货单不被当成收入单、编辑后逐字段不动）；**D26/D26b/D27 云端往返**——测试里内置一个**本机 mock 云端**
  （同端口 `/api/sync/load|save`，按 userId 隔离、只在本进程内存里），走真实 `sync.js` 完成
  「上传 → 清空本机 → 导入同步码 → 拉回 → 再往返一轮」，验证字段、emoji 与两位小数不变且不重复；
  A12-360/390/430 三档布局复验（记单弹窗在货单与收入两种模式下都无容器裁切，类型切换两个按钮
  不重叠、都在弹窗内且点击区 ≥28px 高，收入模式可见两格同宽同左边界）。
  ⑧**留给用户的两条边界**（不是遗漏）：收入单的**金额**在已回款后没有二次入口（与货单同权：删掉重记）
  **【已失效 → 见 v26】**（v26 补上了「改回款」入口，这条边界与货单一起消失）；
  编辑收入单时因垫付是 0，会照旧弹一次「0 元购」确认（编辑一律走货单路径的必然结果，点一次「确定」即可，
  该确认框的文案在 v26 改成同时覆盖两种情形的中性措辞）。
- v26（2026-09-20）：**补两处小改动**——一处真实能力缺口 + 一处文案，都是定点修正（零新字段、
  不改同步协议、视觉语言照旧）。
  ①**已回款的单也能改「回款金额」（真实缺口）**：卡片上的「回款」按钮以前只在 `!isSettled(o)` 时渲染，
  一旦是「已回款」，回款金额与回款日期就**没有任何入口**可改，只能删除重记；而 v25 的**收入单天生
  就是已回款**，所以对这类记录这个缺口 100% 命中（记错金额只能删了重来）。改法刻意复用现有回款弹窗、
  不新做界面：已结算的「**已回款**」单把**同一个按钮**显示为「**改回款**」（同一个 `data-act="pay"`、
  同一个位置与样式），点开的是**同一个 `#payModal`**；打开时按当前记录**预填**（金额填 `o.income`、
  日期填 `o.incomeDate`，标题写「改回款 · 名称」；新建回款仍是「回款 · 名称」＋垫付价＋今天）；
  提交**只写回 `income` 与 `incomeDate` 两个字段**——状态保持「已回款」、**不产生第二条记录**、
  不动 `cost/fee/qty` 与批次字段，利润预览照旧（`income − cost − fee`）。
  判据就是这条记录自己的 `status`，**不去猜它是不是收入单**（v25 定的红线）；
  旧「自留（旧）」单不给这个入口（不是回款语义，保持原样只读）。
  批内单改回款后批次表头会出现「≠ 成员明细合计」提示——**预期**，批次口径属于另一个入口
  （「改本批邮费/回款」），这次一个字都没动它。
  ②**「垫付金额为 0」的确认框改成中性措辞**：v25 起**编辑一条纯收入单**也会走到这个确认（因为编辑
  一律按货单路径、垫付恒为 0），老文案「垫付金额为 0？确认这是 0 元购/白嫖的单子吗？」在那条路上读不通。
  现在写成「**这一单垫付金额为 0（白嫖单或纯收入单），确认保存吗？**」，两种情形都盖住。
  **刻意不为了免掉这次确认去判「这条是不是收入单」**——那正是 v25 明令禁止的脆弱代码
  （任何靠 `cost===0`／字段缺省去识别的写法都会误伤「垫付 0 的已回款老货单」），这次点击原样保留。
  ③**冒烟测试 117 → 130 条**（一条老断言都没删，117 条老断言逐条仍在跑）：新增 13 条 v26 断言——
  已回款**收入单**改回款（按钮文案/样式/data-act 与位置、`#payTitle` 与预填 800.5＋回款日期、
  改成 900 后 `income=900`、**仍是同一条记录**、报表净利润精确 +99.5）；已回款**普通货单**同样能改
  （700 → 650，按回款日期归期后的报表差值 −50）；**未回款**的单按钮文案仍是「回款」、行为不变
  （预填垫付价＋今天、保存后置已回款）；改回款**不动 `cost/fee/qty`/批次字段**（逐字段前后比对，
  差异恰好只有 `income`/`incomeDate` 两项）；0 元确认框的**新文案**（断言弹窗文本含「白嫖单或纯收入单」）
  且**记收入仍不弹**（confirm 计数 0，v25 的 B13 继续绿）；以及**三档布局**
  （360/390/430：卡片上多了「改回款」按钮后仍 0 碰撞、无容器裁切，且该按钮完整落在卡片盒内、
  点击区 ≥28px 高；v24 的 21.1–21.3 也照旧全绿——它们本来就带两张已回款的卡片）。
  按新文案改写了 B14 的描述与附注（断言本身不变，仍是 `/垫付金额为 0/` 命中且正反两向都不误伤）。
- v27（2026-09-20）：**五处定点修正**（用户口径：按推荐的来、不必繁琐）——一处真 bug + 四处易错/缺指引/丢数据风险；
  零新字段、不改同步协议、视觉语言照旧。
  ①**收入口径统一（真 bug）**：看板「累计回款」按「回款非空」判、报表「回款」按「已结算且有回款日期」判，
  于是「已回款 → 编辑成在途（回款字段留着）」这一条被看板算进总收入、报表不算，两页对不上。
  现在两处**共用 `hasIncome()` 这一个函数**，规则一句话：**回款非空即计入收入（与状态无关），
  按回款日期归期**（没有回款日期就只进累计、不落进任何具体月份）；利润跟着收入一起认。
  ②**存成「在途」而这一条还留着回款 → 弹一次确认**（文案讲清后果：垫付会重新算进「垫付在外未回笼」、
  不再进「已结算净盈亏」、那笔回款照旧按回款日期计入收入；点「取消」＝这张表单不保存）。
  判据只有这一条记录自己的 `status` 选择 + 它自己的 `income`，**不做**「这是不是收入单」的判断（v25 红线）。
  ③**编辑弹窗里那行只读回款提示**：编辑「已回款且有回款」的单时，表单里出现一行 `.foot-note`
  「本单回款 ¥X · 改金额请用卡片上的「改回款」」——编辑表单没有回款栏，想改金额最容易错填到「垫付金额」。
  纯展示、不加输入框。④**看板英雄卡提示行补「另有邮费 ¥X」**：在途单自己记的邮费合计（为 0 时不显示这半句），
  与「本月垫出 · 另有邮费」同一个做法；英雄卡那个大字本身只算垫付，**统计口径一分不动**。
  ⑤**批次表头告警补自助指引**：那句「≠ 成员明细合计 …，与整批口径不同」尾部加上
  「要对齐就点「改本批邮费」按当前成员重摊一次」（删单不自动扣整批口径是刻意的边界，但得让用户知道下一步点哪）。
  ⑥**云端拉取不再静默覆盖本机独有的单**：远端 `updatedAt` 更新时，若本机存在「云端没有的订单」（**只按 id 比对**），
  先弹一次确认——「确定」＝把这些单并进账本（默认）、「取消」＝放弃并采用云端版本；并入后 toast 报
  「已并入本机 N 单」。**正常拉取（本机 ⊆ 云端）一次都不弹**；「另一台真删了单」也不会被自动复活（选放弃即可）。
  合并后不立刻回推（下次启动那次拉取的 else 分支自然推上去）。「导入同步码」那条路走 `suppressPullMerge`
  仍用它自己那段按 id 的合并（不重复弹框、合并语义只有一套）。
  ⑦**冒烟测试 130 → 144 条**（一条老断言都没删，130 条老断言逐条仍在跑）：F1 口径统一（造「已回款→改在途」的单，
  断言报表本期回款与看板累计回款对同一笔钱给同一个答案 800.50，报表净盈亏 488.50 = 800.50 − 300 − 12）；
  F2 确认框正/反/取消三条路（正常编辑 0 次）；F3 只读回款提示（已回款的单显示、在途单不显示、弹窗无裁切）；
  F4 英雄卡提示行三种情形（只算在途邮费）；F5 告警里含「改本批邮费」且那个按钮真的在；
  F6/F7/F8 云端冲突（**本机 mock 云端**：并入 3 单不丢、放弃只留云端 2 单、本机 ⊆ 云端时 0 次弹框）；
  F9–F11 三档布局（360/390/430：英雄卡提示行、账本页、编辑弹窗都 0 碰撞 0 裁切 0 自裁）；
  F12 云端段收尾（mock 关掉、端点回死接口，主流程末尾 0.1/0.2 红线照旧成立）。
  测试侧新增两处小机关（只在 v27 段生效，其余段落行为一字不变）：confirm 桩的返回值可经 sessionStorage 控制；
  mock 云端支持 `forced`（无论页面用哪个身份来问都返回那条远端记录）。
- v28（2026-09-21）：**新增「报单」**（用户需求原话：「寄出一批之后要报单，得把型号、颜色、数量统计一下，
  货多的时候得一个个手打」＋「**点一下报单就自动整理好并粘贴到剪贴板，我直接在微信发给对方就好了**」）。
  零新字段、零迁移、不改同步协议、不碰云端。
  ①**入口两个**：批次表头上的「报单」（范围＝这一批全部成员，抬头日期取 `batchDate`）、
  账本筛选行右边的「报单」（范围＝**当前筛选下看得见的单**，所见即所报，抬头取今天）。
  ②**点一下就进剪贴板**：`openBaodan` 里开面板的同时就把文本写进剪贴板并 toast「已复制报单 · N 件 / M 种」，
  面板是「刚才复制了什么」的凭据 + 微调入口（改动后按「复制报单」再复制一次，复制的是**文本框现值**）。
  ③**文本形状照用户发给收货商的那种消息**：首行「月.日 待结」（月日不补零）、下面一行一件、
  **每行行尾都带 ×N**（用户明确要「加上数量」，数量就是 1 也写 ×1）、**没有合计行**。
  ④**分组键 = 归一化后的 name 全等**（全角/不换行空格→普通空格 → 删掉所有空白 → ASCII 小写），
  归一化串**只进 Map**、渲染一律用原始 name；组内**代表写法 = 最早那条单**（按 `createdAt`，缺失补 `"~"`，
  相同比 `id`，保证同一份报单每次显示同一个写法——候选顺序被 `filteredOrders` 那个不稳定 comparator 打乱过）。
  ⑤**件数 = Σqty 而不是订单条数**（`qtyOf` 与卡片/表单同口径：取整、下限 1）。
  ⑥**刻意的边界**：不新增 `model`/`color` 字段、不从 name 里猜着切型号颜色（颜色词表必然误伤
  「红米 Note 13」的数字与「白象方便面」这类名字，且猜错是静默的）；`白`/`白色` 不合并；
  收入单落在范围内也照常当一行报（不猜类型，用户自己取消勾选）。面板全程只读：不写 localStorage、不进同步包。
  （**这条 v31 起已不准确** → 见 v31 的⑥：除快递单号那一格外仍只读，那一格只在**用户自己改了它**时才写。）
  ⑦**修掉一个自查出的真缺陷**（外部审阅独立发现的）：`writeClipboard` 的兜底分支只 `box.select()`
  就无条件 `return true`，**漏了 `execCommand("copy")`** —— 在 `navigator.clipboard` 不可用（http、旧 WebView、
  权限被拒）的设备上复制根本没发生、界面却报「已复制报单」，用户到微信粘出来的是上一批货的内容（静默报错单）。
  现在兜底路径真的执行 `execCommand`、用它的返回值决定提示。
  ⑧**冒烟测试 145 → 169 条**（一条老断言都没删）：32.1 点一下报单＝文本已进剪贴板（面板内容与剪贴板
  逐字节一致）；32.2 文本 = **本文件里独立复算**的期望（分组/件数自己再写一遍，两份实现必须同结论）；
  32.3 Σqty 反证（3 条单数量 1/2/3 必须是 ×6，**×3 不许出现**）；32.4 数量为 1 的行也写 ×1；
  32.5/32.6 同物异写与全角空格并组、且显示原始写法（不含小写无空的归一化串）；
  32.7 比较器自检（把文本改成 ×3 → 期望比对必须报不一致，证明这几条不是恒真）；
  32.8–32.12 勾选联动、全选/全不选双向、空选择时复制只提示不写剪贴板；
  32.13/32.13b/32.13c 复制三条路（clipboard 成功 / **clipboard 被拒走 execCommand 且真的执行了 copy** /
  两条都失败如实报失败）——32.13b/32.13c 做过**变异反证**（把修复退回旧写法 → 两条必红，实测 167 passed 2 failed）；
  32.14 只读性（勾选/改文案/复制后 localStorage 账本一字节未变、没多出同步请求）；
  32.15–32.17 批次表头入口（抬头用批次日期、2 单 1+2 报 ×3、两个按钮不重叠不出表头）、关面板后真的收起；
  32.18 筛选入口跟随当前筛选；32.19–32.21 三档布局（账本页 0 碰撞 0 裁切 + 面板内无裁切）。
  测试侧同时把 `#baodanBtn`/`.list-acts`/`.bh-act`/`.bh-meta-row` 纳入 `KPI_SEL` 版面判据。
  样式侧：`.chips-row` 起可折行 + 两个按钮包进 `.list-acts`（窄屏整盒下移，不拆成一人一行）。
- v29（2026-09-21）：**外部独立验收回来的收尾修正**——一条**真注入**（在 v28 之外的既有渲染路径上）
  ＋ 四处报单粗糙点。改动全部是小定点，零新字段、不改同步协议。
  ①**订单 id 未转义（中）**：`orderCardHtml` 里 5 处 `data-id="${o.id}"` 补 `escapeHtml`。这个值**会从外部数据
  进来**（「导入 JSON 备份」与云端拉取都直接落进 `orders`），一份 id 里带 `">` 的备份文件就能在渲染卡片时
  执行脚本（外部验收实测：`data-id="qX"><img src=x onerror=…>` 真的执行了）。App 自己生成的是
  `crypto.randomUUID()`、从界面点不出这种 id，所以是「引入外部数据才触发」，但修法就是 5 个转义
  （本文件里批次表头与 v28 的报单清单早就这么写了）。
  ②**报单显示名带换行/整串空白（低）**：新增 `bdName()`——换行折成空格、首尾空白去掉、全空白落成「未命名」。
  原先直接用原始 name，一份带换行的脏数据会把「一行一件」的报单切成半截行（实测名字 `冒烟换行
己`
  报出独立一行 `己 ×2`）、全空白名报出 ` ×1` 的空行。
  ③**手改文案后复制仍报旧件数（低）**：件数/种数是从**勾选**重算的，手改过之后与框里内容已脱钩，
  那时不再报数字、只说「已复制 · 你改过的那份」（原先会给一句「已复制 3 件」的假凭据）。
  ④**空范围点报单毫无反应（极低）**：面板照常打开并写「这个范围里没有可报的单」，同时补一句同样的 toast。
  ⑤**非 ISO 的批次日期（`2026-1-5`）抬头静默回落成今天**：`mdShort` 在 `parseDate` 之后再试一次宽松解析
  （`-`→`/`），两条都不成才回落今天——这类日期只会从导入的脏数据进来，实测现在报 `1.5 待结`。
  ⑥**冒烟测试 169 → 178 条**（v29 段 9 条）：脏 id 不执行脚本 + `data-id` 属性值原样往返 + 这张单照样能
  打开编辑（**变异反证：把 `escapeHtml` 退回未转义 → 33.1/33.2 必红，实测 176 passed 2 failed**）；
  脏 batchId 同样安全（锁住既有的转义）；报单里换行名不切行；全空白名落成「未命名 ×1」且无空行；
  脏批次日期报 `1.5 待结`；空范围给 toast。
  ⑦**已知未修、留档（外部验收记录，都不是 v28/v29 引入的）**：(a) 同一份脏数据里两条单**同 id** 时，
  报单清单里勾一条会连带另一条（勾选态按 id 存；只可能来自损坏的文件，暂不为它引入按行索引的新状态）；
  (b) `filteredOrders` 的比较器 `(a,b)=> a.date < b.date ? 1 : -1` 对**同一天**的单两个方向都返回 -1
  （不自洽），所以账本与报单清单里同日多单的**顺序**是引擎相关的——报单**文本**不受影响（组序按
  件数+名称、代表写法按 `createdAt`），实测关掉再打开逐字一致；(c) 名字超 120 字在加载时被截断
  （`normalizeData` 里的既有行为，与报单无关）。
- v30（2026-09-21）：**环图中心金额不再压环**（用户截图投诉「有点丑、数字有遮挡」）。看板
  「订单状态（垫付占比）」与报表「商品利润占比」两张环图共用 `chartDonutSVG`：中心那行金额的字号原先是写死的
  15px，而金额位数是会长的——用户那本账的 `¥13,159.81` 在 15px 下约 89px 宽，内圆（r=44、sw=17）只有 71px，
  数字直接压在圆环上。改法三处：① 环细一点、洞大一点（`sw` 17→13、`r` 44→45，内圆 71→77px）——厚环本身
  也是「有点丑」的来源之一；② 新增 `fitDonutValue()`：按**字宽估算**（数字 0.62、逗号/点 0.33、负号 0.4、
  `¥` 与汉字按 1 算，且只肯用内圆直径的 92%）自动缩号，下限 9.5px；缩不下去就依次退让——先去小数
  （`¥13,160`）、再用万/亿紧凑写法（`¥1.2亿`），**任何金额下都不许压环**；③「垫付合计」那行小字跟着金额
  字号走（不再固定 y=+14），缩号后两行仍挨在一起。
  ④**冒烟测试 178 → 187 条**（v30 段 9 条），判据是**几何的**：把中心那行字的 `getBBox()` 四个角拿去和
  内圆半径（r−sw/2）比，四角必须都落在圆内。覆盖：用户那个数 ¥13,159.81（自动缩号、不再压环）、
  ¥210（**仍是满字号 15**——反向钉住「不许为了保险一律缩小」）、¥123,456,789.12（退让到紧凑写法且仍在环内）、
  报表页那张环图、以及三档宽度（360/390/430）下同一判据 + 看板 0 碰撞 0 裁切。
- v31（2026-09-21）：**报单带上快递单号**（用户要求原话：「报单里面需要带上**日期、快递单号**和明细，
  明细就是类似这种 `狗屁王二代金刚黑 ×5`」——日期与明细 v28 就有，这次加的是单号）。
  新字段一个（`tracking`）、新格子两处、新按钮一个（面板里那个「清空」，沿用既有的 `.fld-clear`），
  批次/邮费/回款的语义一个字没动，同步协议没动。
  ①**字段**：订单新增可选字符串 `tracking`（折空白 + 去首尾空格 + 截 40 字，见下）。
  **关键是它进了 `normalizeData` 的字段白名单**——没列进去的自定义字段会被那个函数**静默丢弃**
  （加载、导入 JSON、云端拉回三条路都只走它），表现会是「记了单号、一刷新就没了」，且全程不报错。
  **白名单与界面侧共用 `cleanTracking` 同一个口径**（修正过的第二版：原先白名单只 `trim+slice`，
  一份带换行的脏值会「账本存 `SF1\nSF2`、卡片与报单文本显示 `SF1 SF2`」——同一份数据两个值，
  而且报单那边可能被切成半截行；现在账本里存的就是界面上会显示的那个规范值）。
  测试为此有「刷新后还在」「云端往返后还在」「云端**拉回**路上的 40 字截断」「脏值三处同一个值」四枪。
  ②**记单/编辑表单**：「更多（状态）」里新增「快递单号（可空）」，是**货单形态专属**（`.kind-order`，
  收入模式下整格隐藏，与邮费/数量同规矩；收入单落库恒 `tracking: ""`）。新建与编辑两条路都写入
  （编辑时回填原值，清空再保存＝去掉）。**「再来一单」刻意不带原单的单号**——那是新的一次寄件，
  沿用旧单号会报错单，比空着严重。
  ③**账本卡片**：`order-mid` 那行末尾补「· 单号 X」（没填的单一个字节都不多）。这一行本就长，
  360px 上多这一截会折行，属正常。
  ④**报单面板**：新增「快递单号」一格 + 「清空」小按钮。
  · **预填** = 这个范围里**最常见的非空单号**（空值不参与统计，否则「最常见」会被一堆空单统治）；
  · 那一格下面的小字**按「本次勾选的单」现算**（不留快照——快照版被独立验收在副本里用真实
    鼠标/键盘打出过一个真缺陷：把这一格改成统一值、写库成功之后，小字还一字不变地说着
    「有 2 个不同的单号…改这一格才会统一」，它已经统一完了。见 ⑦ 的 35.25–35.27 与变异反证 ⑤⑥），
    分三种情形：
    勾选的单里有 ≥2 种非空单号 → 列出来（最多 3 个、多的写「等共 N 个」）+「文本里只带上面那一格的值；
    要统一就改这一格」；勾选的单里只有一种非空单号而这一格填的是别的号 →「这一格填的单号不在这次报的单里
    （它们记的是 X）」；勾选的单一个号都没有（新寄的货还没抄号）或两边一致 → **一个字都不提示**。
    同时开面板那句 toast 在「本次报的单里单号不止一种」时改成「单号有 N 种，先核对面板里的提示再报」
    ——这时自动复制出去的是**众数**，少数派那几单的号不在文本里；
  · **文本**：抬头下面新增一行 `快递单号 X`，**值为空时整行不渲染**（不留空占位行）；
  · 改这一格＝当场重算文本（input）。
  ⑤**写入时机（唯一会写账本的那一处，定稿语义）**：写账本的必要条件是「**用户自己改过那一格**」
  （`baodanTrackingTouched`，在 `change`——blur/回车——与「清空」按钮里置位），在此之上再要求「这次报的那几单里
  有人不是这个值」。用户改完这一格（change）当场走完整一套：写账本（就地改 `tracking` 一个字段 → 既有
  `saveData()`：persist + render + 排程云同步）→ 重算文本 → **重新复制一遍剪贴板** → toast
  「单号已记到 N 单 · 已重新复制」。**打开面板那一下的自动复制一个字都不写**；没动过那一格时点「复制报单」
  也一个字都不写（那一格只在「动过之后勾选集合又变了」时兜一道底：补上新勾进来的单）；
  复制**失败**（`execCommand` 也失败）时同样不写——顺序是先复制成功、才写账本。
  为什么必须是「用户动过」而不只是「值不同」：预填是范围里的众数，少数派单与它**天然不同**，
  只比字符串的话用户「什么都没改、只想再复制一遍」就会把那些单的正确单号统一成众数（静默串单、无处可撤）。
  为什么要在 change 里重新复制：不重复制，剪贴板里还是打开面板那一刻的旧文本（没有新填的号），
  而 toast 还在教他「直接去微信粘贴」。
  ⑥**面板语义改写**：v28 的「面板是只读的」不再准确，手册与测试（32.14）都已改成
  「**除单号那一格外**都是只读的：勾选、改文案不动账本；只有单号那一格在**你自己改了它**时才写」。
  ⑦**冒烟测试 187 → 217 条**（v31 段 30 条，一条老断言都没删）：35.1 打开面板时那一格预填「范围里最常见的
  非空单号」（2 单 SF111 vs 乙/丁/戊 各一个）；35.2 本次报的单里 4 个不同值 → 提示行**只列 3 个** + 「等共 4 个」；
  35.3 开面板的 toast 如实说「单号有 4 种，先核对」（自动复制的是众数）；35.4 文本第 2 行正好是
  「快递单号 SF111」，与测试里**独立复算**的那份**逐行**一致；
  35.5 ★**串单**：没动那一格直接点「复制报单」→ 乙/丁/戊 各自的号一个都没被改写、账本逐字节相同；
  35.6 「清空」＝用户动过这一格：那一行整行消失 + 这批 5 单的单号当场被去掉；
  35.7 手输（只 input）→ 文本当场重算、**账本还是空的**、剪贴板仍是旧文本（等 change 来刷新）；
  35.8 change → 账本写入 + **剪贴板重新复制过**（＝含新单号的那份）+ toast「单号已记到 5 单 · 已重新复制」；
  35.9 值没变再点一次复制 → data 与 meta **逐字节相同**、不多一次同步请求；
  35.10 自检：字节比对对「tracking 被改」是敏感的（parse→stringify 往返一致 + 改一个值必然不同）；
  35.11 超 40 字：那一格自己收敛成 40 字、文本与账本也都是那 40 字（三处同一个值，不只靠 maxlength）；
  35.12 复制**失败**（两条路都挂）→ 账本与 meta 逐字节不变（先复制成功、才写账本）；
  35.13 同一状态复制成功 → 才把新勾回来的那单补写成这个号（复制那一下的兜底分支，只动 tracking）；
  35.14 清空那一格 → 这批 5 单单号全变空、范围外的丙没动；
  35.15 一个单号都没有的范围 → 没有那一行、也没有提示行；
  35.16/35.16b 编辑表单：本来没单号的单那一格是空的、填上保存后账本里就是它；
  **不展开「更多」、什么都不改直接保存，原单号不丢**（编辑是整单重建，回显漏了就会静默清空）；
  35.17 「再来一单」那一格是空的；35.18 这一格是货单形态专属（收入模式隐藏、切回来值还在）；
  35.19 刷新后单号仍在（钉白名单）+ 卡片上写出「· 单号 X」+ 没单号的单不出现「单号」二字；
  35.19b 脏单号（`SF1\nSF2`）加载后**三处同一个值**、随便一次写入落盘后账本里存的也是规范值（白名单口径统一）；
  35.20 本机 mock 云端往返后单号仍在；35.20b **云端拉回**这条路上的 40 字截断（远端 46 字 → 拉回后 40 字）；
  35.21–35.23 三档宽度（360/390/430）下面板与表单这一格都无裁切无碰撞；
  35.25 提示行**现算**：把这一格改成统一值提交后，小字自己消失（不再说着已经失效的话）；
  35.26 取消勾选少数派后，小字改说「这一格填的单号不在这次报的单里（它们记的是 X）」；
  35.27 勾选的单**一个号都没有**时**不给任何提示**（新寄的货还没抄号，最常用的路径不吵人）；
  35.28 全程零 JS 错误。
  **变异反证**（把源码改坏 → 目标断言必红 → 改回来全绿；**七次都在最终源码 md5 `ea2da9e4…` 上做的**，
  每次改完 `node --check` 都过、还原后 md5 一字不差）：
  ① 把 copyBaodan 里「必须先由用户改过」的判据摘掉（退回「比字符串就写」）→ **实测 216 passed / 1 failed，
  红的正是 35.5（串单那一枪）**；② 把 change 里的「重新复制剪贴板」摘掉 → **216 / 1，红的正是 35.8**；
  ③ 把 baodanApplyTracking 里「值没变就不写」的判据摘掉 → **216 / 1，红的是 35.9**；
  ④ 让空值不写（＝清空去不掉单号）→ **211 / 6，红的是 35.6 与 35.14**（35.7/35.15/35.19/35.27 跟着红：
  那几枪的期望建立在「清空真的写进去了」之上）；
  ⑤ 把提示行退回成「开面板那一刻的快照」（＝独立验收打出来的 D1 真缺陷）→ **213 / 3，红的是
  35.25/35.26/35.27**（快照版在三处都说已经失效的话）；⑥ 把 D2b 那条分支（「这一格不在这次报的单里」）摘掉
  → **216 / 1，红的正是 35.26**（35.25/35.27 不受影响，说明这一枪打得准）；⑦ 把白名单退回成只 `trim+slice`
  （D4 之前的两套口径）→ **216 / 1，红的正是 35.19b**（落盘的是 `"SF1\nSF2"`、规范值应是 `"SF1 SF2"`）。
  ⑧**刻意的边界**：不从单号里解析快递公司、不做单号格式校验（顺丰/中通/极兔各有各的写法，
  猜错是静默的）；同一批里几个不同的单号**不自动拆分报单**（只给提示，拆不拆由用户勾选决定）；
  单号不参与去重与统计，报表/看板一个字都没动。
- v32（2026-09-21）：**账本页「一起寄出」批次块可折叠（默认收起）**。用户原话：「把账本中的『批量结算』这里，
  将『一起寄出』变成可折叠的……『一起寄出』作为一个大的卡片，点击展开后，里面是一些分类（即小的选项）」；
  追问后他明确选了「**展开后就是现在这一整套卡片，只是整块能收起**」＋「**默认收起**」。
  只动渲染与一处 CSS，**批次/邮费/回款/分摊/状态的口径一个字没动**，同步协议、数据结构（账本 JSON 字段）
  也一个字节没动——所以这是一次**纯显示层**的改动，老账本零迁移。
  ①**收起态**：只剩第一行「一起寄出 · 日期」+ 右侧「N 单」+ 箭头（收起 ▸ / 展开 ▾）与第二行整批摘要
  （沿用原来的 `bits` 拼法：垫付 · 邮费 · 回款 · N 单在途，0 值项不显示）；两个按钮与成员卡片都不显示。
  **展开态与 v31 一字不动**：`.bh-top` + `.bh-meta-row`（摘要 + 两个按钮）+ 提示行 + 这一批全部成员卡片
  （成员卡片仍是完整卡片、动作按钮照旧）。
  ②**触发区严格只有第一行**：「第一行」做成了真 `<button type="button">`（`div` 在手机上拿不到正确的
  键盘/触摸语义），带 `aria-expanded`；样式抹平成和以前那个 div 一样（整行宽、左对齐、无边框无背景）。
  点这一批的「改本批邮费」「报单」或任何一张卡片上的按钮都**不会**顺手折叠——它们在 `.bh-top` 之外，
  事件委托里 `closest("button[data-act]")` 先命中的是它们自己（36.4 就是钉这一条的，见下面的变异反证）。
  ③**展开/收起只切一个类，不整列表重渲染**：`.batch-block.collapsed` 上的 CSS 负责隐藏成员卡片与提示行，
  箭头用一个字符（▾）靠 CSS 转 −90° 变成 ▸。所以切态**不会**打飞滚动位置或用户此刻手上的状态
  （展开时点「改本批邮费」→ 弹窗开着，一次折叠也不会把它关掉）。
  ④**状态只活在内存**：`expandedBatches`（Set，只记被手动展开的那些批次 id）是展开态的唯一真相——
  `renderList()` 每次都照它拼 `.collapsed` 类，所以**同一次会话里展开过的批次活过任何一次重渲染**
  （改单、回款、切筛选都不重置）；但它**不落库、不进同步包**，刷新页面回到默认收起。
  ⑤**收集起态下「不许被折叠藏起来」的两件事**（第一件是用户在规格里点名的硬要求）：
  · **告警**：有 `≠ 成员明细合计` 告警的批次，收起时摘要行尾部挂一个红色小标记「**⚠ 待对账**」（≤8 字），
    展开后才是完整那句（含 v27 加的「要对齐就点『改本批邮费』按当前成员重摊一次」自助指引）。
    标记只回答「这一批有账要对」，不把一整条长文案塞进摘要行，也绝不把这件事整条藏进展开态。
  · **筛选没说全**（外部验收提的边界）：收起卡上那个「N 单」是**整批**单数，而筛选可能把成员藏起来
    （3 单的一批只看得到 1 单时，光看「3 单」会以为本页有 3 单）。原来那句「本页只显示其中 N 单…」
    在 `.bh-note` 里、收起态看不见——为这个混淆加的话被折叠作废了。修法与告警标记同一套：
    `本页可见成员 < 本批成员` 时，摘要行尾部补一个弱色短标记「**· 本页 N 单**」，展开后仍是原来那句完整文案。
    两个标记并排：**弱色的在前**（只是「本页没显示全」的事实），**红色的在后**（真要动手对账），
    各自 `flex:0 0 auto` + `nowrap`，不重叠不换行（36.15 量了两个盒子不相交、且弱色在左）。
  ⑥**不动的语义**：单成员批次照旧不渲染表头（卡片写「单独一批寄出」）→ 也就没有折叠开关；散单照旧一张张卡片；
  报单/批量结算的候选集合一律按**数据**算（`filteredOrders()`），与页面折不折叠无关
  （36.8 在收起态下点了报单入口验证这一点）。
  ⑦**冒烟测试 217 → 237 条**（v32 段 20 条，一条老断言都没删、也没放宽）：36.1 默认收起（类/`aria-expanded="false"`/
  真 button/摘要行可见/成员卡片 3 张包围盒全 0×0/两个按钮与提示行不可见/无筛选标记）；36.2 点标题行展开（卡片全可见、
  按钮点击区 ≥28px、箭头与 aria 跟着变——判据是 computed transform 从非 none 变 none）；36.3 再点一次收起；
  36.4 点「改本批邮费」不误触发折叠（弹窗开了、aria 不变、卡片仍可见）；36.5 展开态活过一次数据变更
  （编辑 → 全量 render → renderList）与一次筛选重渲染；36.6/36.6b 收起态看得见「⚠ 待对账」、展开态看得见完整文案；
  36.7 散单与单成员批次不受影响（全页 1 个块、1 个开关、2 张直接挂在 #orderList 下的卡片、散单按钮不变）；
  36.8 筛选「在途」时那句「本页只显示其中 2 单」照旧 + 收起态下报单入口仍按数据算出 4 单；
  36.9–36.11 三档宽度（360/390/430）**收起态与展开态都** 0 碰撞 0 裁切（收起 36 个盒子 / 展开 77 个盒子）；
  36.12 折叠状态不落库（localStorage 全键值逐字节不变、不多一次同步请求）；36.13 本段零 JS 错误；
  36.14 刷新页面回到默认收起（新文档里零 JS 错误）；
  **36.15 筛选「已回款」时收起卡上出现「· 本页 1 单」且与「⚠ 待对账」并列不打架**（盒子不相交、弱色在左）；
  36.16 展开后回到原来那句完整文案「本页只显示其中 1 单，另有 2 单被筛选隐藏」、短标记消失、只 1 张成员卡；
  36.17 本页 3 单全可见时那个标记**连元素都不渲染**（不是藏起来）；
  36.18 折叠开关的可达性：带说明性 `title`（含「展开」「收起」）、`aria-expanded` 仍在、**没有**用 `aria-label`
  把「一起寄出 · 日期 3 单」这段可见文本顶掉；36.19 刷新之后这几条同样零 JS 错误。
  **既有断言的适配**（这才是这一版测试侧的主要工作：折叠把「批次内部」变成了默认看不见的东西，
  而 `display:none` 的元素包围盒是 0×0——不处理的话一批断言会失败、另一批会**空转成全绿**。
  做法一律是「**先把那一批展开再测**」，新增测试侧助手 `setBatchExpanded/expandBatch/expandAllBatches`，
  并把 `batchNotes / headState / batchHeadProbe / openBatchFromHead / openBaodanHead` 这五个助手改成自己先展开；
  版面探测（`layoutReport`）在数据里带批次的 4 处（11.1、21.1、27 段 F9–F11、35.21–35.23）也先展开，
  否则那几张成员卡片根本不进探测。**诊断实测（把展开助手改成 no-op 再跑）**：
  11.1 的盒子数从 74 掉到 33、21.1 从 74 掉到 46（＝这些卡片本来会整批漏测）；
  真正会 FAIL 的是 18.0（表头入口的点击区域量出来 0×0）、32.16（两个按钮被判成「跑到表头盒子外面」）、
  33.3（那条这一版**加强**成「展开后按钮真的可见」）；**其余 216 条老断言一字未改、一条没删**。
  **变异反证**（把源码改坏 → 目标断言必红 → 改回来必全绿，三次都在本轮最终源码 md5 `72fa9c06…` 上做，
  改完 `node --check` 都过、还原后 `md5sum -c` 三文件一字不差）：
  ① 把「触发区只有第一行」这条去掉（表头里别的按钮也顺手折叠一下）→ **实测 235 passed / 2 failed，
  红的正是 36.4（点「改本批邮费」后 aria 从 true 变 false、3 张卡片变 0 张可见）与 36.5**
  （它以上一步仍是展开态为前提）；② 把收起态的告警标记摘掉（告警整条藏进展开态）→ **实测 230 / 7，
  红的是 36.6、36.9、36.10、36.11、36.14、36.15、36.17**（都是「收起态要看得见那个标记」的断言）；
  ③ 把收起态的「· 本页 N 单」标记摘掉（那句解释继续只活在展开态）→ **实测 236 / 1，红的正是 36.15**
  （收起卡只有「3 单」、本页实际 1 单却没有第二句话说清）。
  ⑧**刻意不做的边界**（别下一个人以为漏了）：
  · **折叠状态不持久化**（不写 localStorage、不进同步包，刷新即收起）；
  · **中性陈述不给标记**：「本批已有 N 单不在其中（退出或删除）」这类 `warn:false` 的历史陈述，收起态
    **既不显示、也不给标记**——它是「当初结了几单」的历史信息（成员数本身就反映了现状），展开即见；
    **两个标记只覆盖「当下必须知道」的两件事**：`⚠ 待对账` 只认 `warn:true` 的金额告警，
    `· 本页 N 单` 只认「本页可见成员 < 本批成员」。既不藏、也不把整行塞满。
  · 不做「展开/收起全部」按钮、不做展开动画、不做记住上次的展开（用户只要「默认收起、点一下展开」）；
  · 收起态**不隐藏整批金额**（摘要行照旧写垫付/邮费/回款/N 单在途）——收的是**成员明细**，不是账目信息；
  · 也不因为折叠而少渲染任何数据（成员卡片始终在 DOM 里，只是不显示）。
- 测试残留：开发验证时向 Worker 写过一条测试身份的账本（2 笔订单，随机 UUID 隔离），
  无影响，无法也无需清理
